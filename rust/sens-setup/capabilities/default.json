{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "La ventana del instalador no trae marco del sistema: arrastrar, minimizar y cerrar son suyos, y aparece cuando la página ya pintó. El selector de carpetas elige dónde instalar.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-minimize",
    "core:window:allow-close",
    "core:window:allow-show",
    "dialog:allow-open"
  ]
}
